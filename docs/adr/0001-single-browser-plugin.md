# 用一个插件替代原版 native

为让调用 agent 的 `agent_browser` 和 Jev 委托共用同一浏览器状态，我们发布一个包含 native fork 与 Jev 委托功能的 Pi 插件，替代原版 native 安装。原版工具名和直接调用方式保持不变；不同时加载两份 native，不绕过其浏览器执行路径，也不修改 Pi 核心。与两个独立安装包或另起浏览器相比，这种方式把状态归属和安装切换保持为一个整体。
