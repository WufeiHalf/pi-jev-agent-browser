# Jev 浏览器委托

Pi 中可调用工具的 agent 把一段浏览器操作交给 Jev，并在同一浏览器中接手后续操作和验收。

## Language

**浏览器插件**：
安装在 Pi 中的一个插件，同时提供原有的 `agent_browser` 和新增的 `jev_browser`。它替代原版 native 插件，不与原版同时加载。
_避免_：两个插件、第二个浏览器插件

**调用 agent**：
当前调用工具的 Pi agent，例如当前会话中的助手。它决定每一段目标，并负责最终验收。
_避免_：开发 agent、内置 agent、父 agent

**分段目标**：
调用 agent 一次交给 Jev 的多步浏览器任务，有明确的停止位置。下一次委托是新目标，不继承 Jev 上次任务的上下文。
_避免_：完整流程、继续上次任务

**交接**：
Jev 停止本段任务后，调用 agent 在同一个浏览器页面接手；调用 agent 也可以再发起一个新目标。
_避免_：切换浏览器、恢复任务

**验收**：
调用 agent 检查浏览器中的实际结果，并判断 E2E 是否通过。Jev 表示目标完成不等于验收通过。
